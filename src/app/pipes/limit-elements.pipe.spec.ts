import { LimitElementsPipe } from './limit-elements.pipe';

describe('LimitElementsPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitElementsPipe();
    expect(pipe).toBeTruthy();
  });
});
